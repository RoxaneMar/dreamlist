<div class="marged">
  <div class="wrapper-profile">
    <div class="container">
      <div class="row">
        <%= render 'shared/profile_show', user: @dreams.first.user %>
        <% Dream::CATEGORIES.each do |category| %>
          <div class="col-xs-12 col-sm-4">
            <%= render 'shared/tableau', user: @dreams.first.user, category: category %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
